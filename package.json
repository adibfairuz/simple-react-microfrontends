{
  "name": "simple-react-microfrontends",
  "private": true,
  "workspaces": {
    "packages": [
      "host",
      "app1",
      "app2"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.1.5"
  }
}
