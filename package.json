{
  "name": "simple-react-microfrontends",
  "private": true,
  "workspaces": {
    "packages": [
      "host",
      "app1",
      "app2"
    ]
  },
  "scripts": {
    "kill-ports": "kill-port --port 8080,3001,3002",
    "start": "npm run kill-ports && lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "kill-port": "^2.0.0"
  }
}
